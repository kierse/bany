rootProject.name = 'bany'

include 'domain', 'adapter'

include ':api'
findProject(':api')?.name = 'bany-plugin-api'

include 'plugins:sample'
findProject(':plugins:sample')?.name = 'sample-plugin'

include 'plugins:simplii'
findProject(':plugins:simplii')?.name = 'simplii-plugin'

